import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles({
  root: {
    maxWidth: '100%',
    margin: '5em auto 2em',
    width: '854px',
    padding: '15px'
  }
})
