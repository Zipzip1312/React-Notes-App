import { makeStyles } from '@material-ui/core/styles'

export const useStyles = makeStyles({
  root: {
    maxWidth: '854px',
    width: '100%',
    margin: '5em auto 2em',
    padding: '15px'
  }
})
